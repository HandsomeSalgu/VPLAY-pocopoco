<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pocopoco_vplay.payment.model.mapper.PaymentMapper">
	<insert id="insertPaymentInfo">
		insert into payment(PAYMENT_DATE, PAYMENT_AMOUNT, USER_NO) values(#{paymentDate},#{amount},#{userNo})
	</insert>
</mapper>